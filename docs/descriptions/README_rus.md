# C++ Messenger

[![C++ Standard](https://img.shields.io/badge/C%2B%2B-17+-00599C?style=flat&logo=c%2B%2B&logoColor=white)](https://en.cppreference.com/w/cpp/17)
[![Qt Version](https://img.shields.io/badge/Qt-6+-41CD52?style=flat&logo=qt&logoColor=white)](https://www.qt.io/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-16.3-316192?style=flat&logo=postgresql&logoColor=white)](https://www.postgresql.org/)
[![License](https://img.shields.io/badge/License-Free_to_Use-green.svg)](LICENSE)
[![Maintenance](https://img.shields.io/badge/Maintained%3F-yes-green.svg)](https://github.com/dimbo1324)

> **Кроссплатформенный мессенджер с клиент-серверной архитектурой, построенный на C++(17) и Qt6. Включает консольный клиент/сервер, GUI для администратора и хранение в PostgreSQL.**

---

### 🌐 Документация / Documentation / Documentación

* 🇺🇸 [**English (Английский)**](../../README.md)
* 🇪🇸 [**Spanish (Español)**](README_sp.md)

---

## 📖 Обзор

**C++ Messenger** — это готовый к производству шаблон для создания безопасного, масштабируемого приложения для обмена сообщениями. Он следует чистой архитектуре, разделяя сетевые взаимодействия (TCP-сокеты), многопоточность, работу с базой данных и компоненты интерфейса.

Независимо от того, изучаете ли вы C++ или разрабатываете систему чата в реальном времени, этот проект предоставляет необходимую инфраструктуру — кроссплатформенные сокеты, пулы потоков, интеграцию с PostgreSQL и административный интерфейс на Qt6 — чтобы вы могли сосредоточиться на добавлении функций, таких как шифрование или поддержка медиафайлов.

### ✨ Ключевые особенности

* **Кроссплатформенная поддержка:** Работает на Linux и Windows благодаря абстракциям для сокетов (POSIX/WinSock) и потоков.
* **Многопоточный сервер:** Обработывает несколько подключений с использованием ThreadPool для эффективного распределения задач.
* **Qt6 GUI для администратора:** Мониторинг пользователей, фильтрация сообщений, управление банами/киками, с темами (тёмная/светлая).
* **Хранение в PostgreSQL:** Безопасное хранение пользователей, сообщений, логов и статусов онлайн с использованием libpqxx.
* **Консольный клиент/сервер:** Регистрация, вход, обмен сообщениями, списки пользователей и просмотр истории.
* **Повторно используемые библиотеки:** `tcp` для сокетов и `threading` для пулов и логирования.
* **Расширяемый дизайн:** Легко добавить шифрование, медиафайлы, push-уведомления или unit-тесты.

---

## 🛠️ Технологический стек

* **Язык:** [C++ (17)](https://en.cppreference.com/w/cpp/17)
* **Фреймворк UI:** [Qt6](https://www.qt.io/)
* **База данных:** [PostgreSQL](https://www.postgresql.org/)
* **Драйвер базы данных:** [libpqxx](https://github.com/jtv/libpqxx)
* **Система сборки:** [CMake](https://cmake.org/)
* **Сеть:** POSIX sockets / WinSock
* **Многопоточность:** std::thread / std::mutex
* **Другие инструменты:** pkg-config (для Linux/macOS), Git

---

## 🚀 Начало работы

Следуйте этим шагам, чтобы запустить локальную копию. Это руководство предназначено для новичков — даже если вы новы в IT, мы объясним каждый шаг подробно.

### Предварительные требования

Перед началом установите эти инструменты. Ссылки на скачивание предоставлены.

* **Git**: Для клонирования проекта. Скачайте с [git-scm.com](https://git-scm.com/downloads). Установите и добавьте в PATH.
* **CMake**: Версия 3.21 или выше. Скачайте с [cmake.org](https://cmake.org/download/). Установите и добавьте в PATH.
* **Компилятор C++**:
  - **Windows**: Visual Studio 2019/2022 (Community-версия бесплатна). Скачайте с [visualstudio.microsoft.com](https://visualstudio.microsoft.com/downloads). Выберите нагрузку "Разработка десктопных приложений с C++".
  - **Linux (Ubuntu/Debian)**: Установите GCC/Clang через терминал: `sudo apt update && sudo apt install g++ cmake`.
  - **macOS**: Установите Xcode из App Store, затем Command Line Tools: `xcode-select --install`.
* **Qt6**: Версия 6 или выше (для GUI). Скачайте установщик с [qt.io/download](https://www.qt.io/download-qt-online-installer). Установите компоненты "Desktop" для вашей платформы (например, msvc2019_64 на Windows).
* **PostgreSQL**: Сервер базы данных. Скачайте с [postgresql.org/download](https://www.postgresql.org/download/). Установите и запомните пароль суперпользователя (по умолчанию: "postgres").
* **libpqxx**: Библиотека C++ для PostgreSQL.
  - **Linux**: `sudo apt install libpqxx-dev`.
  - **macOS**: `brew install libpqxx` (сначала установите Homebrew: `/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"`).
  - **Windows**: Скачайте исходники с [github.com/jtv/libpqxx](https://github.com/jtv/libpqxx), соберите с CMake, или используйте vcpkg: Установите vcpkg, затем `vcpkg install libpqxx:x64-windows`.

Проверьте установки:
- Откройте терминал/командную строку и выполните: `git --version`, `cmake --version`, `g++ --version` (или `cl` на Windows), `psql --version`.

### 1. Клонирование репозитория

Откройте терминал/командную строку и выполните:

```bash
git clone https://github.com/dimbo1324/c-plus-plus-messenger.git
cd c-plus-plus-messenger
```

Это скачает проект на ваш компьютер.

### 2. Настройка окружения

Проект использует переменные окружения для настройки. Установите их в терминале перед сборкой/запуском.

| Переменная        | Описание                             | Значение по умолчанию                  |
| ----------------- | ------------------------------------ | -------------------------------------- |
| `CHAT_DB_CONN`    | Строка подключения к БД              | `host=localhost port=5432 dbname=chat_db user=postgres password=your_password` |
| `QT_PATH`         | Путь к установке Qt6 (для CMake)     | (Установите, если не в PATH, напр., `C:/Qt/6.5.3/msvc2019_64`) |
| `CMAKE_BUILD_TYPE`| Тип сборки (Release/Debug)           | `Release`                              |

На Windows: `set CHAT_DB_CONN=host=localhost port=5432 dbname=chat_db user=postgres password=your_password`

На Linux/macOS: `export CHAT_DB_CONN="host=localhost port=5432 dbname=chat_db user=postgres password=your_password"`

Замените `your_password` на ваш пароль PostgreSQL.

### 3. Запуск базы данных

Запустите службу PostgreSQL:
- **Windows**: В меню Пуск найдите "Службы", найдите "postgresql-x64-16", правой кнопкой > Запустить.
- **Linux**: `sudo systemctl start postgresql`
- **macOS**: `brew services start postgresql`

Проверьте: Выполните `psql -U postgres` и введите пароль. Введите `\q` для выхода.

### 4. Миграция базы данных

Создайте базу данных и таблицы.

1. Откройте psql: `psql -U postgres`
2. Создайте БД: `CREATE DATABASE chat_db;`
3. Подключитесь: `\c chat_db`
4. Выполните SQL-скрипт из `root/app/database/init.sql`. Скопируйте содержимое в psql или выполните: `psql -U postgres -d chat_db -f root/app/database/init.sql`
5. (Опционально) Добавьте тестовые данные: `psql -U postgres -d chat_db -f root/app/database/sample_data.sql`

Создайте выделенного пользователя для безопасности:
- В psql: `CREATE USER chat_user WITH PASSWORD 'secure_password';`
- Дайте права: `GRANT CONNECT ON DATABASE chat_db TO chat_user; \c chat_db GRANT USAGE ON SCHEMA public TO chat_user; GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA public TO chat_user;`

Обновите `CHAT_DB_CONN`, чтобы использовать `chat_user` и `secure_password`.

### 5. Сборка проекта

Создайте папку для сборки, чтобы сохранить чистоту:

```bash
mkdir build
cd build
```

Настройте CMake:
- **Windows** (в "x64 Native Tools Command Prompt for VS"):
  ```bat
  cmake -G "Visual Studio 16 2019" -A x64 -DQT_PATH="C:/Qt/6.5.3/msvc2019_64" ..
  ```
- **Linux/macOS**:
  ```bash
  cmake -DCMAKE_BUILD_TYPE=Release ..
  ```

Соберите:
```bash
cmake --build . --config Release
```

Это создаст исполняемые файлы: `chat_client`, `chat_server`, `server_gui`.

---

## 📂 Структура проекта

Проект следует модульной структуре:

```text
c-plus-plus-messenger/
├── .gitignore
├── LICENSE
├── README.md
├── assets/                   # Ресурсы (например, изображения для GUI)
│   ├── 1.png
│   └── ...
├── CMakeLists.txt            # Корневой CMake: Подключает все подпроекты
└── root/
    ├── app/
    │   ├── client/           # Консольный клиент
    │   │   ├── CMakeLists.txt
    │   │   ├── include/      # Заголовки клиента
    │   │   │   ├── Client.h
    │   │   │   └── UI.h
    │   │   └── src/          # Исходники клиента
    │   │       ├── main.cpp
    │   │       ├── Client.cpp
    │   │       └── UI.cpp
    │   ├── server/           # Консольный сервер
    │   │   ├── CMakeLists.txt
    │   │   ├── include/      # Заголовки сервера
    │   │   │   ├── ClientHandler.h
    │   │   │   ├── Database.h
    │   │   │   ├── Logger.h
    │   │   │   └── Server.h
    │   │   └── src/          # Исходники сервера
    │   │       ├── main.cpp
    │   │       ├── Server.cpp
    │   │       ├── ClientHandler.cpp
    │   │       ├── Database.cpp
    │   │       └── Logger.cpp
    │   ├── libs/             # Общие библиотеки
    │   │   ├── tcp/          # Абстракция TCP-сокетов
    │   │   │   ├── CMakeLists.txt
    │   │   │   ├── include/
    │   │   │   │   └── tcp/
    │   │   │   │       ├── ISocket.h
    │   │   │   │       ├── SocketFactory.h
    │   │   │   │       ├── TcpSocketLinux.h
    │   │   │   │       └── TcpSocketWin.h
    │   │   │   └── src/
    │   │   │       ├── SocketFactory.cpp
    │   │   │       ├── TcpSocketLinux.cpp
    │   │   │       └── TcpSocketWin.cpp
    │   │   └── threading/    # ThreadPool и Logger
    │   │       ├── CMakeLists.txt
    │   │       ├── include/
    │   │       │   ├── threading_config.h
    │   │       │   └── threading/
    │   │       │       ├── Logger.h
    │   │       │       └── ThreadPool.h
    │   │       └── src/
    │   │           ├── Logger.cpp
    │   │           └── ThreadPool.cpp
    │   ├── database/         # SQL-скрипты
    │   │   ├── init.sql
    │   │   ├── sample_data.sql
    │   │   └── README.md
    │   └── logs/             # Логи сервера
    │       └── user_activity.log
    └── serverGUI/            # Qt6 GUI для администратора
        ├── CMakeLists.txt
        ├── include/
        │   └── MainWindowServer.h
        ├── src/
        │   ├── main.cpp
        │   └── mainwindowserver.cpp
        └── ui/
            └── MainWindowServer.ui
```

---

## 🔌 Использование

### Сервер

```bash
cd build/root/app/server/Release  # или Debug
./chat_server 8080
```

Вывод: "Server listening on port 8080". Логи в `../logs/user_activity.log`.

### Клиент

```bash
cd build/root/app/client/Release
./chat_client 127.0.0.1:8080
```

Следуйте меню: Регистрация/Вход, затем отправка сообщений, просмотр списков/истории.

### GUI администратора

```bash
cd build/root/serverGUI/Release
./server_gui
```

Обновляйте пользователей/сообщения, фильтруйте, баните/кикайте. По умолчанию использует заглушки (отредактируйте CMake, чтобы отключить).

---

## 🤝 Вклад

Вклады — это то, что делает сообщество open-source таким удивительным местом для обучения, вдохновения и создания. Любой ваш вклад будет **очень ценен**.

1. Форкните проект
2. Создайте ветку для фичи (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

---

## 📜 Лицензия

Этот проект свободен для использования.

---

## 📬 Контакты

Если у вас есть вопросы, предложения или вы просто хотите поздороваться, не стесняйтесь обращаться!

* **Автор:** dimbo1324
* **Telegram:** [@dimbo1324](https://t.me/dimbo1324)
* **Email:** dimaprihodko180@gmail.com
* **GitHub:** [github.com/dimbo1324](https://github.com/dimbo1324)

---

*Разработано с ❤️ от dimbo1324*