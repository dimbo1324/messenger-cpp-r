# Console Chat Application ✨Local homework🚀

## 1. Ветка 🔀
Проект развивается в ветке: **dev_hw_sf_18_12_11-HM-03**  

## 2. Задача 🎯
- Написать `Makefile`, который собирает консольный чат.  
- Дополнить чат сохранением информации о зарегистрированных пользователях и истории сообщений в файлы, чтобы при повторном запуске восстанавливалось предыдущее состояние чата.  

## 3. Решение 💡
- Реализованы классы `Chat`, `User`, `Message`.  
- При старте программа читает данные о пользователях и сообщениях из файла `chat_state.json` (через библиотеку [nlohmann/json](https://github.com/nlohmann/json)).  
- При выходе данные сериализуются в JSON и сохраняются, чтобы при следующем запуске восстановить текущее состояние.  
- Сборка осуществляется с помощью `Makefile`. Поддерживаются цели `all`, `clean`, `run`, `install`, `uninstall`, `help`.  

## 4. Применяемые библиотеки 📚
- **Стандартная библиотека C++**: `<iostream>`, `<fstream>`, `<string>`, `<unordered_map>`, `<vector>`, `<memory>` и др.  
- **[nlohmann/json](https://github.com/nlohmann/json)**: для сериализации/десериализации данных в формате `*.json`.  

## 5. Дерево проекта 🌳
```
console-chat-by-Cpp/
├── app/
│   ├── main.cpp
│   └── src/
│       ├── Chat/
│       │   ├── Chat.cpp
│       │   ├── Chat.h
│       │   └── utils/
│       │       ├── Display.cpp
│       │       ├── Display.h
│       │       ├── Exceptions.h
│       │       ├── Input.cpp
│       │       ├── Input.h
│       │       ├── Time.cpp
│       │       └── Time.h
│       ├── Message/
│       │   ├── Message.cpp
│       │   └── Message.h
│       └── User/
│           ├── User.cpp
│           └── User.h
├── include/
│   └── nlohmann/
│       └── json.hpp
├── Makefile
└── README.md
```

## 6. Параметры компиляции ⚙️
В `Makefile` заданы следующие основные параметры:
CXX = g++
CXXFLAGS = -Wall -Wextra -O2 -std=c++23 -I./include -I./app/src
- `-Wall -Wextra` — включены все основные предупреждения.  
- `-O2` — оптимизация сборки.  
- `-std=c++23` — используем современный стандарт C++23.  
- `-I./include -I./app/src` — пути для заголовочных файлов.  

## 7. Разработчик 👨‍💻
- **Приходько Дмитрий Викторович**  
Студент группы **CPLUS48** онлайн-школы IT-профессий **Skillfactory**  

## 8. Контакты 📱
- Сот: +7 904 926‒57‒29  
- Telegram: [@dimbo1324](#)  
- Email: [dim4dmi7rij@yandex.ru](mailto:dim4dmi7rij@yandex.ru)  
- Доп. почта: [dimaprihodko180@gmail.com](mailto:dimaprihodko180@gmail.com)  